version: 2

sources:
    - name: jaffle_shop
      description: This is a replica of our raw data from the Snowflake database
      database: raw
      schema: jaffle_shop
      tables:
          - name: customers
            description: One record per customer.
          - name: orders
            description: One record per order. Includes cancelled and deleted orders.